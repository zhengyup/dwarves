spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/dwarves
    username: survey_user
    password: survey_pass
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true

server:
  port: 8080